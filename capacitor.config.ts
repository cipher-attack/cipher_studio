import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cipher.studio',
  appName: 'Cipher Elite',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
